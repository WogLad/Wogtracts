<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Contract</title>

    <style>
        body {
            background-color: #121212;
            color: #dfdfdf;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            /* text-shadow: 10px -1px -1px rgb(214, 214, 214); */
        }

        h2 {
            text-align: center;
            vertical-align: middle;
            margin-top: 5px;
            background: linear-gradient(90deg, rgb(0, 181, 51) 0%, rgb(0, 130, 37) 100%);
            -webkit-text-fill-color: transparent;
            background-clip: text;
            -webkit-background-clip: text;
        }

        button {
            padding: 8px;
            position: relative;
            vertical-align: top;
            color: white;
            cursor: pointer;
            text-align: center;
            font-weight: bold;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.25);
            background: #00551f;
            /* background: #c0392b; */

            border: 0;
            border-radius: 6px;
            border-bottom: 4px solid #002e08/*#b53224*/;

            box-shadow: inset 0 -2px #002e08/*#b53224*/;
            -webkit-box-shadow: inset 0 -2px #002e08/*#b53224*/;
            -moz-box-shadow: inset 0 -2px #002e08/*#b53224*/;

            transition: all 0.2s linear;
            -webkit-transition: all 0.2s linear;
            -moz-transition: all 0.2s linear;
        }
        button:active {
            top: 2px;
            -webkit-top: 2px;
            outline: none;
            -webkit-outline: none;
            box-shadow: none;
            -webkit-box-shadow: none;
            border-bottom: 3px solid #002e08;
            -webkit-border-bottom: 3px solid #002e08;
        }
        button:disabled {
            cursor: not-allowed;
            opacity: 60%;
        }
    </style>
</head>
<body style="display: flex; align-items: center; flex-direction: column;">
    <script>
        function getContractDiv() {
            var el = document.createElement("div");
            
            // The header part of the contract div
            el.innerHTML = `<h2><u>What do you want to create?</u></h2>`;
            el.innerHTML += "<hr width='200px' style='margin-top: -20px;'>";

            // The styling part of the contract div
            el.style.margin = "auto";
            el.style.marginTop = "10%";
            el.style.border = "2px #007500 solid";
            el.style.borderRadius = "10px";
            el.style.maxWidth = "450px";
            el.style.height = "300px";
            el.style.backgroundColor = "#242424";
            el.style.boxShadow = "rgb(0 0 0) 0px 0px 15px 5px";
            el.style.paddingLeft = "15px";
            el.style.textAlign = "center";
            el.style.paddingRight = "13px";
            // el.style.display = "flex";
            // el.style.flexDirection = "column";

            // The body part of the contract div
            el.append(document.createElement("br"));

            var newTransactedItemClassButton = document.createElement("button");
            newTransactedItemClassButton.innerHTML = "New TransactedItem Class";
            el.append(newTransactedItemClassButton);

            el.append(document.createElement("br"));
            el.append(document.createElement("br"));
            
            var newContractButton = document.createElement("button");
            newContractButton.innerHTML = "New Contract";
            el.append(newContractButton);

            return el;
        }

        document.body.append(getContractDiv());
    </script>

    <!-- <h2>What do you want to make?</h2>
    <div style="display: flex; flex-direction: column;">
        <button>New TransactedItem Class</button>
        <br>
        <button>New Contract</button>
    </div> -->
</body>
</html>